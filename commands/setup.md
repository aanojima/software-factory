---
description: Initialize, update, migrate, or inspect Software Factory project configuration without symlinks.
argument-hint: [init|update|status] [--to ref] [--opencode] [repo]
---

Use the `factory-setup` skill for:

`$ARGUMENTS`

Run the scripts bundled with this plugin. Do not create a home-directory or
PATH symlink and do not require a separate source checkout.
