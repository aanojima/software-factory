---
description: Ticket to mergeable PR in one command — classify, implement, review, open a PR, then hand off unattended CI/review triage.
argument-hint: [ticket description, Linear key (ENG-1234), or Linear URL]
---

Use the `implement-ticket` skill for:

`$ARGUMENTS`
