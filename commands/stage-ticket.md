---
description: Ticket to a staged commit on a branch — classify, implement, verify — then stop before opening a PR.
argument-hint: [ticket description, Linear key (ENG-1234), or Linear URL]
---

Use the `stage-ticket` skill for:

`$ARGUMENTS`
