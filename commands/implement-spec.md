---
description: Implement an authoritative specification through guarded exploration, planning, validation, and review.
argument-hint: [path/to/spec.md]
---

Use the `implement-spec` skill to implement the authoritative specification at:

`$ARGUMENTS`

The current session orchestrates the workflow and dispatches the plugin-bundled implementation-worker for implementation and repairs. The host may write only for the documented genuinely trivial DIRECT change or when native delegation is unavailable. Use native read-only subagents for exploration, conformance review, and risk-specific review as required. Preserve durable artifacts under `.agent-runs/` and honor every stop condition in the skill.
