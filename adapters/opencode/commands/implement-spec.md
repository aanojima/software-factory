---
description: Implement an authoritative specification through the portable guarded workflow
agent: build
---

Load and use the `implement-spec` skill for the authoritative specification at `$ARGUMENTS`.

Remain the parent orchestrator and sole writer. Delegate bounded factual research to `repo-explorer`, then obtain independent review from `conformance-reviewer` and, when required, `security-reviewer`.
